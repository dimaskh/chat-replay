// Prefix
export const PREFIX = 'modules/users'

// Actions
export const RECEIVE_USERS = `${PREFIX}/RECEIVE_USERS`
export const REMOVE_USER = `${PREFIX}/REMOVE_USER`
export const UPDATE_USER = `${PREFIX}/UPDATE_USER`
