// Prefix
export const PREFIX = 'modules/player'

// Actions
export const REPLAY_RECORD = `${PREFIX}/REPLAY_RECORD`
export const RECEIVE_RECORD = `${PREFIX}/RECEIVE_RECORD`
export const FINISH_RECORD = `${PREFIX}/FINISH_RECORD`
export const CLEAR_DATA = `${PREFIX}/CLEAR_DATA`
