// Prefix
export const PREFIX = 'modules/messages'

// Actions
export const RECEIVE_MESSAGE = `${PREFIX}/RECEIVE_MESSAGE`
export const REMOVE_MESSAGE = `${PREFIX}/REMOVE_MESSAGE`
export const UPDATE_MESSAGE = `${PREFIX}/UPDATE_MESSAGE`
